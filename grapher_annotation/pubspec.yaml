name: grapher_annotation
description: Annotation package for Grapher code generation.
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  gql: ^1.0.0
  graphql: ^5.0.0
  meta: ^1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
